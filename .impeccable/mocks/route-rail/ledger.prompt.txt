Design compositional option 3 of 3 for the first viewport of the real Chinese desktop web application “Flacier Rules”, 1440×900 landscape. Maintain the exact same combined world as the references: the atlas contributes cool off-white paper, deep navy/teal palette, editorial rule index, flat thin-rule material, and typography character; the railway contributes only functional interlocking lines, junctions, and signal-state feedback. Do not copy either reference layout. This is an operational proxy routing rule and subscription tool, not a marketing page or a railway hobby interface.

OPTION 3 — 路线台账. Use a route-first ledger topology built for scanning many rules. A thin header shows “Flacier Rules” with links “网址测试”, “规则”, “订阅”, “转换”, “GitHub”. Directly below, a wide compact command/result band occupies the full width: URL input “https://api.openai.com/v1/models”, action “检查路线”, and on the same band the current four-step result “api.openai.com” → “AI.list” → “AI” → “美国节点” connected by a small teal interlocking line. Labels above the four steps read “DOMAIN / RULE SET / POLICY / EXIT”.

The main region below is a dense rule ledger spanning about 70% width. Rows: AI, Steam, Discord, DEV, Media, Social, Bilibili, AniGamer. Columns: “规则集 / 默认策略 / 国家出口 / 更新”. Use a thin horizontal track through each row, with a filled teal signal marker at the active policy and tiny junctions where a row can switch between DIRECT, GLOBAL, and country nodes. AI is selected and expanded by one quiet detail row showing “OpenAI / Claude / Codex / Grok”. Steam shows two route choices: “下载：DIRECT” and “联机：DIRECT（可切换代理）”. Bilibili has its own row. AniGamer shows “台湾节点”. Do not invent metrics or update dates.

A narrower right rail titled “当前策略” shows selectable countries “香港 / 新加坡 / 日本 / 台湾 / 美国”, 美国 selected; a small legend for AI, DEV, MEDIA, SOCIAL; and factual notes “默认：GLOBAL” and “进程规则需在客户端检查”. At the bottom edge, a quiet subscription strip begins with an URL field and client labels “Mihomo / sing-box / Surge / Stash / Shadowrocket / Loon / Quantumult X”.

Make the ledger the strongest element, not the title. Use cool off-white field, navy ink, teal selection and active routes, pale blue-green row highlight, muted red only for stop/error. No orange, yellow, gold, gradients, glow, blur, black cyberpunk, glassmorphism, floating rounded SaaS cards, shadows, oversized headline, hero stats, decorative section numbers, kicker copy, emoji, locomotives, station photos, tickets, or third-party logos. Workhorse condensed grotesk for section headings, neutral grotesk for UI, mono only for URLs/codes. Thin rules, square corners, disciplined density, implementable with semantic table/grid plus SVG route marks in React/CSS/SVG.
