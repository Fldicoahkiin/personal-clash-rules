Design compositional option 2 of 3 for the first viewport of the real Chinese desktop web application “Flacier Rules”, 1440×900 landscape. Fix the same combined design world as the two references: the atlas reference provides restrained off-white paper, navy/teal palette, editorial index, line weights, typography character, and flat material; the railway reference contributes only functional interlocking tracks, junctions, and signal-state feedback. Do not copy their layouts. This is an operational proxy routing and subscription tool, not a landing page or railway-themed entertainment.

OPTION 2 — 调度工作台. Use a clear three-region application topology below a thin top nav. Top nav: “Flacier Rules”, “网址测试”, “规则”, “订阅”, “转换”, “GitHub”. Immediately below is a compact URL test command bar containing “https://api.openai.com/v1/models” and action “检查路线”. The rest of the first viewport is a joined workbench, not floating cards:

LEFT, about 22% width: a rule-set index with selectable rows AI, Steam, Discord, DEV, Media, Social, Bilibili, AniGamer. Show AI selected, with small file code “AI.list”.
CENTER, about 52% width and visually dominant: a precise interlocking route canvas. The active track runs “api.openai.com” → “AI.list” → “AI” → “美国节点”. Use filled teal signal dots for the active path, thin navy sidings for alternatives, and small muted red stop dots for rejected branches. Place functional captions “请求主机 / 命中规则 / 路由策略 / 国家出口” along the route. The diagram must be readable as product logic, not decorative rail art.
RIGHT, about 26% width: an inspector with four fixed rows “域名 / 规则集 / 策略 / 出口”, their real current values, then a compact country switch “香港 / 新加坡 / 日本 / 台湾 / 美国” with 美国 selected. Show “默认：GLOBAL” and the note “进程规则需在客户端检查”.

At the bottom edge, reveal the beginning of a single subscription import drawer with client names “Mihomo / sing-box / Surge / Stash / Shadowrocket / Loon / Quantumult X”. It should prove the same visual grammar can continue below the fold.

Color/material: cool off-white paper or enamel field, deep navy ink, teal active state, pale blue-green selections, muted red only for stop/error. No orange, yellow, gold, gradient, blur, glow, black cyberpunk, glassmorphism, rounded SaaS card grid, drop shadows, metrics, emoji, decorative section numbers, kicker copy, huge marketing headline, locomotives, station photos, tickets, or third-party logos. Modern condensed workhorse grotesk for headings; mono only for URL and codes. Thin 1px rules, square corners, high density only in the side indexes. Make it implementable using semantic React/CSS/SVG.
