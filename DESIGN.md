# Design

## Direction

订阅转换是一页操作工具。冷白底、深蓝文字和青色操作状态；不使用橙色、装饰插画、投影或大圆角。

页面顺序：

1. 订阅转换
2. 规则测试
3. 规则集
4. 规则格式转换
5. 策略组

## Type

- 标题：自托管 Flacier Display，回退到 Noto Sans SC
- 正文：系统无衬线
- URL、规则、策略和输出：系统等宽字体

## Components

- 输入和按钮使用 2px 小圆角，工作区保持方正。
- 主按钮使用青色；绿色和红色只表达通过与未通过，并同时显示文字。
- 客户端选择同时显示官方图标和名称。
- GitHub 入口使用 Primer 官方 Mark。
- 规则测试以一条普通细线连接“输入网址、命中规则、处理结果”，三个值必须完整显示。
- 表格和列表用细边框分隔，不把每一项做成独立卡片。

## Responsive

- 820px 以下改为单列，规则处理线改为竖向。
- 520px 以下隐藏页内导航，按钮与结果操作占满可用宽度。
- 不因屏幕变窄删减规则、状态或目标格式。
